class Solution {
    public int scoreOfString(String s) {
        int result = 0;
        char[] chars = s.toCharArray();
        int n = chars.length;
        for (int i = 0; i < n - 1; i++) {
            result += Math.abs(chars[i] - chars[i + 1]);

        }
        return result;
    }
}
